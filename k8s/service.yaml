apiVersion: v1
kind: Service
metadata:
  name: zomato-app
spec:
  type: NodePort
  selector:
    app: zomato-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31403
